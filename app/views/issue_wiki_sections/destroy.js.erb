<% if @issuewikisections.destroyed? %>
  $('#iws-<%= @issuewikisections.id.to_s %>').remove();
<% else %>
  alert('<%= raw(escape_javascript(l(:notice_failed_to_detele_iws,
   :errors => @project.errors.full_messages.join(', ')))) %>');
<% end %>
